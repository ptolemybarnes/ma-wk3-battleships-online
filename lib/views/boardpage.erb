<!DOCTYPE html>
<html>
<head>
</head>

<body>

<% if params[:same_shot_error] %>
  You can't shoot at the same cell twice!
<% elsif params[:miss_shot] %>
  You MISS!
<% end %>

 <div id="board">
  
  <table style="width: 100%">
    <% @keys.each_slice(10) do |slice| %>
      <tr>
        <% slice.each do |key|%>
        <td>
          <form action="/boardpage" method='post'>
            <input type="image" id="<%= key.to_s %>" src="/images/water.gif" value="<%= key.to_s %>" name="shot" style="width: 100%">
          </form>
        </td>
        <%end%>
      </tr>
    <%end%>
  </table>
 </div>



</body>
</html>
