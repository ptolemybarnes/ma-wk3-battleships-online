<!DOCTYPE html>
<html>
<head>

</head>

<body>

  <div id="board">
<table>
    <% @keys.each_slice(10) do |slice| %>
  <tr>
      <% slice.each do |key|%>
      <td>
        <form action="/boardpage">
        
        <input type="image" id="<%= key.to_s %>" src="./images/water.gif" value="<%= key.to_s %>" name="shot">
        
        </form>

        </td>
        <%end%>
  </tr>
      <%end%>

  </table>

  </div>

<% if params[:shot] %>
You MISS!
<% end %>
</body>

</html>
